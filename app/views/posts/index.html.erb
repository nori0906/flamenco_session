<h1>posts#index</h1>

<div>
  <%= link_to "新規作成", new_post_path %>

</div>

<div>
  <p>投稿一覧</p>
  <% @posts.each do |p| %>
    <p>タイトル</p>
    <span><%= p.title %></span>
    <span><%= p.body %></span>
    <p>内容</p>
    
  <% end %>
</div>

<div class="record">
  <div>
    <%# <p>音声一覧</p> %>
    <%#= audio_tag( "tempfile.webm", controls: true) %>
  </div>
</div>


<%= javascript_pack_tag 'main' %>